# Copy to /etc/davy-jones/env (referenced by deploy/davy-jones.service).
# See backend/.env.example for the full list with explanations -- this is
# just the minimum a production install typically wants to set explicitly.

DJ_HOST=127.0.0.1
DJ_PORT=8080
DJ_DATA_DIR=/var/lib/davy-jones
